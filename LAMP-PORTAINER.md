# 🐳 Docker LAMP Stack สำหรับ Portainer

ชุด **Docker Stack** สำหรับสร้าง Web Server (LAMP Stack) ที่ออกแบบมาเพื่อใช้งานบน **Portainer** โดยเฉพาะ พร้อมระบบ SFTP ที่ไม่มีปัญหา Permission และ phpMyAdmin สำหรับจัดการฐานข้อมูล

## 🚀 ฟีเจอร์หลัก

* **Web Server:** Apache + PHP 8.2 (พร้อม mysqli, pdo_mysql)
* **Database:** MySQL 8.0
* **Database Management:** phpMyAdmin
* **File Management:** SFTP Server (ไม่มีปัญหา Permission)
* **ออกแบบมาเพื่อ Portainer:** ใช้งานง่าย Deploy ได้ทันที

## � ความต้องการเบื้องต้น

* Portainer CE/EE เวอร์ชัน 2.x ขึ้นไป
* Docker Engine รันอยู่บน Server
* สิทธิ์ในการสร้าง Stack บน Portainer

## 🚀 การติดตั้งบน Portainer

### วิธีที่ 1: Deploy จาก Git Repository (แนะนำ)

1. **เข้าสู่ Portainer** และเลือก Environment ที่ต้องการ
2. ไปที่เมนู **Stacks** → คลิก **+ Add stack**
3. ตั้งชื่อ Stack เช่น `lamp-stack`
4. เลือกวิธี **Repository**
   - **Repository URL:** `https://github.com/dev04th/LAMP-Stack`
   - **Repository reference:** `refs/heads/main` (หรือ branch ที่ต้องการ)
   - **Compose path:** `docker-compose.yml`
5. ในส่วน **Environment variables** ให้เพิ่มตัวแปรต่อไปนี้:

```bash
MYSQL_ROOT_PASSWORD=your_secure_root_password
MYSQL_DATABASE=my_database
MYSQL_USER=my_user
MYSQL_PASSWORD=your_secure_password
WEB_PORT=80
PMA_PORT=8080
SFTP_PORT=2222
SFTP_USER=dev
SFTP_PASSWORD=your_sftp_password
```

6. คลิก **Deploy the stack**
7. รอให้ Container ทั้งหมดรันเสร็จ (ดูได้จากสถานะเป็นสีเขียว)

### วิธีที่ 2: Deploy แบบ Manual Upload

1. **ดาวน์โหลดไฟล์** จาก Repository
   - `docker-compose.yml`
   - `Dockerfile`

2. **เข้าสู่ Portainer** และเลือก Environment
3. ไปที่ **Stacks** → **+ Add stack**
4. ตั้งชื่อ Stack
5. เลือกวิธี **Upload**
   - อัปโหลด `docker-compose.yml`

6. เลื่อนลงไปยังส่วน **Environment variables**
7. คลิก **+ add an environment variable** และเพิ่มตัวแปรทีละตัว:

| Name | Value | คำอธิบาย |
|------|-------|----------|
| `MYSQL_ROOT_PASSWORD` | `your_password` | รหัสผ่าน Root ของ MySQL |
| `MYSQL_DATABASE` | `my_app` | ชื่อฐานข้อมูลที่จะสร้างอัตโนมัติ |
| `MYSQL_USER` | `my_user` | ชื่อ User ที่จะสร้างอัตโนมัติ |
| `MYSQL_PASSWORD` | `user_password` | รหัสผ่านของ User |
| `WEB_PORT` | `80` | Port สำหรับเข้าเว็บไซต์ |
| `PMA_PORT` | `8080` | Port สำหรับ phpMyAdmin |
| `SFTP_PORT` | `2222` | Port สำหรับ SFTP |
| `SFTP_USER` | `dev` | Username สำหรับ SFTP |
| `SFTP_PASSWORD` | `sftp_password` | รหัสผ่านสำหรับ SFTP |

8. คลิก **Deploy the stack**

### วิธีที่ 3: ใช้ไฟล์ .env (Advanced)

1. สร้างไฟล์ `.env` บนเครื่องของคุณตามตัวอย่าง:

```ini
# Database Configuration
MYSQL_ROOT_PASSWORD=MySecureRootPass123!
MYSQL_DATABASE=production_db
MYSQL_USER=app_user
MYSQL_PASSWORD=SecureUserPass456!

# Port Configuration
WEB_PORT=80
PMA_PORT=8080
SFTP_PORT=2222

# SFTP Configuration
SFTP_USER=developer
SFTP_PASSWORD=SftpPass789!
```

2. ใน Portainer เมื่อสร้าง Stack ให้เลือก **Load variables from .env file**
3. อัปโหลดไฟล์ `.env` ที่สร้างไว้
4. Deploy the stack

## 🖥️ การเข้าใช้งาน

หลังจาก Deploy สำเร็จ คุณจะสามารถเข้าถึงบริการต่างๆ ได้ตาม Port ที่กำหนด:

| บริการ | URL | ข้อมูล Login |
|---------|-----|--------------|
| **Website** | `http://SERVER_IP:80` | - |
| **phpMyAdmin** | `http://SERVER_IP:8080` | User: `MYSQL_USER`<br>Password: `MYSQL_PASSWORD` |
| **SFTP** | `sftp://SERVER_IP:2222` | User: `SFTP_USER`<br>Password: `SFTP_PASSWORD` |

> **หมายเหตุ:** แทน `SERVER_IP` ด้วย IP Address ของเซิร์ฟเวอร์จริง

## � การจัดการไฟล์ผ่าน SFTP

### ตั้งค่า SFTP Client (FileZilla, WinSCP, Cyberduck)

**ตัวอย่างการตั้งค่าใน FileZilla:**

1. **Host:** IP ของเซิร์ฟเวอร์
2. **Username:** `dev` (หรือตามที่ตั้งใน `SFTP_USER`)
3. **Password:** ตามที่ตั้งใน `SFTP_PASSWORD`
4. **Port:** `2222` (หรือตามที่ตั้งใน `SFTP_PORT`)
5. **Protocol:** SFTP

### การอัปโหลดไฟล์

1. เชื่อมต่อผ่าน SFTP Client
2. ลากไฟล์เว็บไซต์ (เช่น `index.php`) ใส่ในโฟลเดอร์ `upload`
3. เปิดเว็บเบราว์เซอร์ที่ `http://SERVER_IP:80`
4. ไฟล์จะแสดงผลทันที **ไม่ต้องแก้ Permission**

### ทำไมไม่มีปัญหา Permission?

* SFTP Server ใช้ UID `33` ซึ่งตรงกับ Apache User (`www-data`)
* ไฟล์ที่อัปโหลดจะมีเจ้าของเป็น `www-data` อัตโนมัติ
* PHP สามารถอ่าน/เขียน/ลบไฟล์ได้ทันที
* **ไม่ต้อง chmod 777** ซึ่งไม่ปลอดภัย

## 🔧 การตั้งค่าเพิ่มเติม

### เปลี่ยน Port ของบริการ

หากต้องการเปลี่ยน Port (เช่น Web จาก 80 เป็น 8000):

1. ใน Portainer ไปที่ **Stacks** → เลือก Stack ของคุณ
2. คลิก **Editor**
3. แก้ไขค่า Environment variable `WEB_PORT` จาก `80` เป็น `8000`
4. คลิก **Update the stack**
5. เลือก **Re-pull images and redeploy**

### เพิ่ม Volume สำหรับสำรองข้อมูล

ปัจจุบัน Stack ใช้ Named Volumes อัตโนมัติ:
- `web_data` → เก็บไฟล์เว็บ
- `db_data` → เก็บฐานข้อมูล MySQL
- `sftp_data` → เก็บไฟล์ SFTP

คุณสามารถดู Volumes ได้ที่ Portainer → **Volumes**

### ปรับแต่ง PHP Configuration

1. สร้างไฟล์ `php.ini` ตัวอย่าง:

```ini
upload_max_filesize = 50M
post_max_size = 50M
max_execution_time = 300
memory_limit = 256M
```

2. อัปโหลดไฟล์ไปยัง Server
3. แก้ไข `docker-compose.yml` ในส่วน `web` service:

```yaml
web:
  volumes:
    - web_data:/var/www/html
    - /path/to/php.ini:/usr/local/etc/php/php.ini
```

4. Update Stack ใน Portainer

## 📊 การตรวจสอบสถานะ

### ผ่าน Portainer UI

1. ไปที่ **Stacks** → เลือก Stack ของคุณ
2. ดู Container status (ควรเป็น **running** ทั้งหมด)
3. คลิกที่ Container แต่ละตัวเพื่อดู Logs

### ตรวจสอบ Logs

1. ใน Stack view คลิกที่ Container name (เช่น `lamp-stack_web_1`)
2. คลิกแท็บ **Logs**
3. ดู error messages (ถ้ามี)

### Container ที่ควรจะรันอยู่

✅ `web` - Apache + PHP  
✅ `db` - MySQL  
✅ `phpmyadmin` - phpMyAdmin  
✅ `sftp` - SFTP Server

## 🧹 การจัดการ Stack

### Restart Stack

1. ไปที่ **Stacks** → เลือก Stack
2. คลิกปุ่ม **Stop**
3. รอให้หยุดเสร็จ → คลิก **Start**

### อัปเดต Stack

1. คลิกปุ่ม **Editor**
2. แก้ไข `docker-compose.yml` หรือ Environment variables
3. คลิก **Update the stack**
4. เลือก **Re-pull images and redeploy** (ถ้าต้องการดึง image ใหม่)

### ลบ Stack

> ⚠️ **คำเตือน:** การลบ Stack จะลบ Container แต่ **Volumes จะยังคงอยู่**

1. ไปที่ **Stacks** → เลือก Stack
2. คลิกปุ่ม **Delete**
3. ถ้าต้องการลบ Volumes ด้วย ให้ไปที่ **Volumes** และลบทีละรายการ

## 🔒 คำแนะนำด้านความปลอดภัย

### สำหรับ Production

1. **เปลี่ยนรหัสผ่านทั้งหมด** จาก default ให้เป็นรหัสผ่านที่แข็งแรง
2. **ตั้ง Port ที่ไม่ธรรมดา** (ไม่ใช่ 22, 80, 3306)
3. **ใช้ Firewall** จำกัดการเข้าถึงเฉพาะ IP ที่ไว้วางใจ
4. **อัปเดต Images** เป็นประจำ
5. **สำรองข้อมูล Volumes** เป็นประจำ

### การสำรองข้อมูล

```bash
# สำรอง Database Volume
docker run --rm -v lamp-stack_db_data:/data -v $(pwd):/backup \
  alpine tar czf /backup/db_backup_$(date +%Y%m%d).tar.gz /data

# สำรอง Web Files Volume
docker run --rm -v lamp-stack_web_data:/data -v $(pwd):/backup \
  alpine tar czf /backup/web_backup_$(date +%Y%m%d).tar.gz /data
```

## 📞 การแก้ปัญหา

### Container ไม่ขึ้น (Failed to start)

1. ตรวจสอบ Logs ของ Container
2. ดูว่า Port ซ้ำกับบริการอื่นหรือไม่
3. ตรวจสอบ Environment variables ว่าครบถ้วนหรือไม่

### ไม่สามารถเข้า phpMyAdmin ได้

1. รอให้ Container `db` รันเสร็จก่อน (ประมาณ 30 วินาที)
2. ตรวจสอบว่า Port `8080` ไม่ถูกใช้งานโดยบริการอื่น
3. ลอง Restart Container `phpmyadmin`

### SFTP ต่อไม่ได้

1. ตรวจสอบว่า Container `sftp` รันอยู่
2. เช็ค Firewall ว่าเปิด Port `2222` หรือไม่
3. ตรวจสอบ Username/Password ให้ถูกต้อง

### Website ไม่แสดงผล PHP

1. ตรวจสอบว่าไฟล์มีนามสกุล `.php`
2. ดู Logs ของ Container `web`
3. ตรวจสอบว่าไฟล์อยู่ใน Volume `web_data` จริง

## 📚 เอกสารเพิ่มเติม

* [Portainer Documentation](https://docs.portainer.io/)
* [Docker Compose Reference](https://docs.docker.com/compose/compose-file/)
* [PHP Official Docker Image](https://hub.docker.com/_/php)
* [MySQL Docker Image](https://hub.docker.com/_/mysql)

## 🎯 Next Steps

หลังจาก Deploy สำเร็จแล้ว คุณสามารถ:

1. อัปโหลดไฟล์เว็บผ่าน SFTP
2. สร้าง Database และ Tables ผ่าน phpMyAdmin
3. เริ่มพัฒนา Web Application ของคุณได้เลย!

---

**เวอร์ชัน:** 1.0.0  
**อัปเดตล่าสุด:** 2026-01-13  
**ผู้พัฒนา:** [Your Name/Organization]
