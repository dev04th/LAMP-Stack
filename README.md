# 🐳 Docker LAMP Stack + phpMyAdmin + SFTP

โปรเจกต์นี้คือชุด **Docker Compose** สำหรับสร้าง Web Server (LAMP Stack) ที่พร้อมใช้งาน โดยออกแบบมาให้รองรับการ **SFTP** เข้าไปแก้ไขไฟล์ได้ทันทีโดยไม่ติดปัญหา Permission (Permission Denied) เหมาะสำหรับการนำไป Deploy บน **Portainer** หรือเครื่อง Server ทั่วไป

## 🚀 ฟีเจอร์หลัก (Features)

* **OS/Web Server:** Linux + Apache (Base Image `php:8.2-apache`)
* **Database:** MySQL 8.0 (Native Password Plugin)
* **Language:** PHP 8.2 (พร้อม `mysqli`, `pdo`, `pdo_mysql`)
* **Management:** phpMyAdmin สำหรับจัดการฐานข้อมูล
* **File Access:** SFTP Server แยก (User ตรงกับ Apache `www-data`)

## 📂 โครงสร้างไฟล์ (File Structure)

```text
.
├── docker-compose.yml    # ไฟล์ Config หลัก
├── Dockerfile            # สำหรับ Build PHP Image
├── .env                  # ไฟล์เก็บค่าตัวแปรและความลับ (ห้ามเอาขึ้น Git)
├── .env.example          # ไฟล์ตัวอย่างสำหรับคนอื่นดู
└── README.md             # คู่มือการใช้งาน
```

## 🛠️ การติดตั้ง (Installation)

### 1. Clone Repository

```bash
git clone https://github.com/dev04th/LAMP-Stack.git
cd LAMP-Stack
```

### 2. ตั้งค่า Environment Variables

ให้สร้างไฟล์ `.env` จากไฟล์ตัวอย่าง:

```bash
cp .env.example .env
```

จากนั้นแก้ไขไฟล์ `.env` เพื่อกำหนดรหัสผ่านและ Port ตามต้องการ:

```ini
# Database
MYSQL_ROOT_PASSWORD=secret_root_pass
MYSQL_DATABASE=my_app
MYSQL_USER=my_user
MYSQL_PASSWORD=secret_user_pass

# Ports
WEB_PORT=80
PMA_PORT=8080
SFTP_PORT=2222

# SFTP Credential
SFTP_USER=dev
SFTP_PASSWORD=dev_pass
```

### 3. รัน Container

**สำหรับ Docker Compose ปกติ:**

```bash
docker-compose up -d --build
```

**สำหรับ Portainer:**

1. สร้าง Stack ใหม่
2. เลือก Repository (ถ้าเชื่อม Git) หรือ Upload ไฟล์ `docker-compose.yml`, `Dockerfile`
3. ใส่ค่าใน Environment variables (หรือโหลดไฟล์ `.env`)
4. กด Deploy the stack

## 🖥️ การเข้าใช้งาน (Usage)

| บริการ | URL / Host | หมายเหตุ |
|---------|------------|----------|
| Website | `http://IP-Address:80` | (หรือ Port ที่ตั้งใน `WEB_PORT`) |
| phpMyAdmin | `http://IP-Address:8080` | Login ด้วย Root หรือ User ที่สร้างไว้ |
| SFTP | `sftp://IP-Address:2222` | Host คือ IP ของ Server |

## 🔑 ข้อมูลสำหรับ SFTP Client (เช่น FileZilla, WinSCP)

* **Protocol:** SFTP
* **Host:** IP Address ของเครื่อง Server
* **Port:** `2222` (หรือตามค่า `SFTP_PORT`)
* **User:** `dev` (หรือตามค่า `SFTP_USER`)
* **Password:** (ตามค่า `SFTP_PASSWORD`)
* **Path:** เมื่อ login เข้าไป ให้ลากไฟล์ใส่ในโฟลเดอร์ `upload` ไฟล์จะไปปรากฏที่หน้าเว็บทันที

## ⚠️ หมายเหตุสำคัญ (Important Notes)

### เรื่อง Permission ของ SFTP

ใน `docker-compose.yml` บริการ SFTP ถูกตั้งค่าไว้ด้วยคำสั่ง:

```yaml
command: ${SFTP_USER}:${SFTP_PASSWORD}:33
```

* เลข `33` คือ User ID (UID) ของ `www-data` (Apache User)
* ทำให้ไฟล์ที่อัปโหลดผ่าน SFTP มีเจ้าของเป็น `www-data` โดยอัตโนมัติ
* PHP สามารถอ่าน/เขียน/ลบไฟล์เหล่านั้นได้ทันที
* **ไม่ต้อง** ทำการ `chmod 777` ให้ไม่ปลอดภัย

### การปรับแต่ง PHP (php.ini)

หากต้องการแก้ไขค่า `php.ini` (เช่น `upload_max_filesize`) แนะนำให้สร้างไฟล์ `php.ini` แล้ว Mount เข้าไปใน Container ผ่าน `docker-compose.yml`:

```yaml
web:
  volumes:
    - ./php.ini:/usr/local/etc/php/php.ini
```

## 🧹 การลบ (Clean up)

หากต้องการลบ Container และ Network (ข้อมูลใน Volume จะยังอยู่):

```bash
docker-compose down
```

หากต้องการลบข้อมูลทั้งหมดรวมถึง Database (**Warning:** ข้อมูลหายหมด):

```bash
docker-compose down -v
```

---

### ข้อแนะนำเพิ่มเติม

คุณควรสร้างไฟล์ `.env.example` เตรียมไว้ด้วยนะครับ โดยก๊อปปี้ Key ทั้งหมดมาจาก `.env` แต่ลบค่า Password จริงออก เพื่อให้คนอื่น (หรือตัวคุณเองในอนาคต) รู้ว่าต้องตั้งค่าอะไรบ้าง

**Next Step:** คุณต้องการให้ผมเขียนไฟล์ `.env.example` ให้ด้วยเลยไหมครับ หรือจะให้ช่วยเรื่องการตั้งค่า **Custom php.ini** เพิ่มเติม?
